{% extends 'base.html.twig' %}

{% block body %}
    <h1>ArticuloVenta</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ articuloVentum.id }}</td>
            </tr>
            <tr>
                <th>Cantidad</th>
                <td>{{ articuloVentum.cantidad }}</td>
            </tr>
            <tr>
                <th>Precio</th>
                <td>{{ articuloVentum.precio }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('articuloventa_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('articuloventa_edit', { 'id': articuloVentum.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
