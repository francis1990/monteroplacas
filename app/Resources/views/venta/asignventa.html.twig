
{% extends 'sistemadmin.html.twig' %}
{% block breadCrumb %}
    {{ parent() }}
    <li>
        <a href="{{ path('venta_index') }}">Venta</a>
    </li>
    <li>
        Editar
    </li>
{% endblock %}
{% block contenido %}        
            <div class="row">
                <div class="col-sm-12 col-md-12">
                    <h3 class="heading">Asignar Venta a Nota de Crédito</h3>
                </div>
            </div>
             <div class="form_error">{{form_errors(form)}}</div>
            <div class="row" id="content">
                <div class="col-sm-12 col-md-12">
                    {{ form_start(form,{'attr':{ 'class':'form-horizontal form_validation_reg'}}) }}
                    <div class="formSep">
                        <div class="row">
                            <div class="col-xs-12">
                                <fieldset>
                                    {#<div class="form-group">
                                        <label class="control-label col-sm-2">{{ form_label(form.documento) }}</label>
                                        <div class="col-sm-8">
                                            {{ form_widget(form.documento) }}
                                            {{ form_errors(form.documento) }}
                                        </div>
                                    </div>#}
                                   {# <div class="form-group">
                                        <label class="control-label col-sm-2">Serie</label>
                                        <div class="col-sm-8">
                                            <select id="venta_seriedoc" class="form-control"></select>
                                        </div>
                                    </div>#}
                                    {#<div class="form-group">
                                        <label class="control-label col-sm-2">{{ form_label(form.seriedoc) }}</label>
                                        <div class="col-sm-8">
                                            {{ form_widget(form.seriedoc) }}
                                            {{ form_errors(form.seriedoc) }}
                                        </div>
                                    </div>#}
                                    
                                    <div class="form-group">
                                        <label class="control-label col-sm-2">Serie - Número de Factura</label>
                                        <div class="col-sm-8">
                                            {{ form_widget(form.venta) }}
                                            {{ form_errors(form.venta) }}
                                        </div>
                                    </div>
                                        
                                </fieldset>
                            </div>
                        </div>
                    </div>
                    <div class="formSep form-actions">
                        <a class="btn btn-default" href="{{ path('venta_index') }}"><span class="glyphicon glyphicon-arrow-left"></span> Volver</a>
                        <button class="btn btn-primary" type="submit"><span class="glyphicon glyphicon-save"></span> Guardar</button>
                    </div>
                    {{ form_end(form) }}
                </div>
            </div>

{% endblock %}